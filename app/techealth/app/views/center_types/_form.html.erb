<%= form_with(model: center_type, local: true) do |form| %>
  <% if center_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(center_type.errors.count, "error") %> prohibited this center_type from being saved:</h2>

      <ul>
      <% center_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
