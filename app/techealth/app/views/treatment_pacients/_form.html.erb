<%= form_with(model: treatment_pacient, local: true) do |form| %>
  <% if treatment_pacient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(treatment_pacient.errors.count, "error") %> prohibited this treatment_pacient from being saved:</h2>

      <ul>
      <% treatment_pacient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_patient %>
    <%= form.number_field :id_patient %>
  </div>

  <div class="field">
    <%= form.label :dosis %>
    <%= form.text_field :dosis %>
  </div>

  <div class="field">
    <%= form.label :days %>
    <%= form.number_field :days %>
  </div>

  <div class="field">
    <%= form.label :secundary_effects %>
    <%= form.text_field :secundary_effects %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
